import 'package:flutter/material.dart';

class Inventario extends StatelessWidget {
  const Inventario({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Inventario page'),),
    );
  }
}